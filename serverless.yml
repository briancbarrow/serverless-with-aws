service: fem-course

provider:
  name: aws
  runtime: nodejs8.10
  profile: personal
  region: us-east-1
  stage: dev

plugins:
  - serverless-offline

functions:
  api:
    handler: src/api/index.handler
    events:
      - http: ANY /
      - http: "ANY /{param+}"
